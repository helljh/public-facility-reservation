spring:
  application:
    name: publicfacility

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}